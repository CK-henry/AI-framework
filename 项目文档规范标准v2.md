# 项目文档规范标准 v2.0

> 版本：2.0.0
> 更新日期：2026-01-29
> 适用范围：所有新项目（语言无关）

---

## 概述

本规范定义了一套**分层的、自描述的、支持语义索引的项目文档体系**。

### v2.0 核心改进

| 特性 | v1.0 | v2.0 |
|------|------|------|
| 文档格式 | 纯 Markdown | **YAML 头部 + Markdown** |
| 索引方式 | 手动层级遍历 | **自动语义索引** |
| 定位精度 | 文件级 | **Section 级** |
| 维护成本 | 手动维护 | **自动生成** |
| 语言支持 | 特定语言 | **语言无关** |
| Token 消耗 | ~10,000 字符 | **~2,000 字符** |

### 核心组件

```
项目文档体系
├── SKILL.md          # 分层的导航和 API 参考文档
├── SKILL.index.json  # 自动生成的语义索引
├── build-index-auto.js  # 索引生成工具
└── docs/             # 集中的设计文档和开发计划
```

---

## SKILL.md 规范

### 最小格式（极简版）

只需要 **3 行 YAML 头部**，工具自动提取其他信息：

```markdown
---
name: module-name
tags: [标签1, 标签2, 标签3]
---

# 模块名称

## 模块概述
...

## API 文档

### functionName
功能说明...
```

**自动提取**：
- 从 `##` `###` `####` 标题提取 sections
- 从标题文字提取 tags（中文词 + 英文词）
- 自动计算行号范围

### 完整格式（推荐）

```markdown
---
name: auth
title: 认证模块
tags: [登录, 认证, Token, JWT, 用户验证, 密码, 注册]
---

# Auth 认证模块

> 层级：第 3 层（子模块文档）
> 上层：[src/SKILL.md](../SKILL.md)

## 模块概述

- **职责**：处理用户认证相关的所有功能
- **位置**：src/modules/auth/
- **依赖**：core/database, core/logger, core/error

## 模块架构

```
auth/
├── SKILL.md              # 本文档
├── index.ts              # 模块入口
├── controllers/
│   └── auth.controller.ts
├── services/
│   └── auth.service.ts
├── models/
│   └── token.model.ts
├── types/
│   ├── login.dto.ts
│   └── register.dto.ts
└── __tests__/
    └── auth.service.test.ts
```

### 设计模式

- **Service 模式**：业务逻辑封装在 AuthService 中
- **DTO 模式**：使用 Data Transfer Object 进行数据验证

### 关键概念

- **JWT Token**：使用 JSON Web Token 进行身份验证
- **Refresh Token**：支持 Token 刷新机制
- **Password Hash**：使用 bcrypt 进行密码加密

## API 文档

### AuthService

#### login(dto: LoginDto): Promise<TokenPair>

**功能说明**：用户登录，验证凭据并返回 Token

**参数**：
- `dto` (LoginDto): 登录数据传输对象
  - `email` (string): 用户邮箱
  - `password` (string): 用户密码

**返回值**：
- (Promise<TokenPair>): Token 对象
  - `accessToken` (string): 访问令牌
  - `refreshToken` (string): 刷新令牌

**抛出异常**：
- `ValidationError`: 当参数验证失败时
- `UnauthorizedError`: 当凭据无效时

**使用示例**：

```typescript
import { AuthService } from '@/modules/auth';

const authService = new AuthService();

const tokens = await authService.login({
  email: 'user@example.com',
  password: 'password123',
});

console.log(tokens.accessToken);
```

**注意事项**：
- 密码错误超过 5 次会锁定账户 15 分钟
- accessToken 有效期为 15 分钟
- refreshToken 有效期为 7 天

---

#### register(dto: RegisterDto): Promise<User>

**功能说明**：用户注册，创建新用户账户

...（其他 API 同样格式）

## 模块开发规范

### 错误处理

```typescript
// ✅ 正确：使用统一的 ErrorHandler
import { ErrorHandler } from '@/core/error';
import { Logger } from '@/core/logger';

try {
  const user = await this.validateCredentials(email, password);
} catch (error) {
  Logger.error('Login failed', { email, error: error.message });
  ErrorHandler.handle(error, { context: 'AuthService.login' });
  throw error;
}
```

### 日志记录

```typescript
// ✅ 正确：使用统一的 Logger
import { Logger } from '@/core/logger';

Logger.info('User logged in', { userId: user.id });
Logger.warn('Login attempt failed', { email, reason: 'invalid password' });
Logger.error('Authentication error', { error, context });
```

### 数据验证

```typescript
// ✅ 正确：使用 Zod 进行验证
import { z } from 'zod';

const LoginSchema = z.object({
  email: z.string().email('Invalid email format'),
  password: z.string().min(1, 'Password is required'),
});

export type LoginDto = z.infer<typeof LoginSchema>;
```

## 测试要求

- 单元测试覆盖率：> 80%
- 测试文件位置：`__tests__/`
- 必须测试的场景：
  - 正常登录流程
  - 密码错误处理
  - Token 刷新
  - 账户锁定机制

## 依赖关系

### 外部依赖

- `jsonwebtoken`: JWT 生成和验证
- `bcrypt`: 密码加密
- `zod`: 数据验证

### 内部依赖

- `@/core/database`: 数据库操作
- `@/core/logger`: 日志记录
- `@/core/error`: 错误处理
- `@/modules/user`: 用户信息查询

## 相关文档

- [认证设计文档](../../../docs/design/auth-design.md)
- [API 文档](../../../docs/api/auth-api.md)

## 常见问题

1. **Token 过期后如何处理？**
   使用 refreshToken 方法获取新的 Token 对。

2. **如何实现"记住我"功能？**
   延长 refreshToken 的有效期至 30 天。
```

---

## 语义索引系统

### 索引文件结构

```json
{
  "version": "3.0.0",
  "mode": "auto-extract",
  "updated": "2026-01-29T10:00:00.000Z",
  "stats": {
    "totalFiles": 5,
    "totalModules": 2,
    "totalSections": 32,
    "totalTags": 61
  },
  "modules": {
    "auth": {
      "file": "src/modules/auth/SKILL.md",
      "title": "认证模块",
      "tags": ["登录", "认证", "Token", "JWT"],
      "sections": {
        "模块概述": {
          "level": 2,
          "title": "模块概述",
          "start": 38,
          "end": 43,
          "tags": ["模块概述"]
        },
        "sub:login": {
          "level": 4,
          "title": "login(dto: LoginDto): Promise<TokenPair>",
          "start": 79,
          "end": 120,
          "tags": ["login", "dto", "logindto", "promise", "tokenpair"]
        }
      }
    }
  },
  "tagIndex": {
    "登录": ["auth"],
    "login": ["auth#sub:login"],
    "Token": ["auth", "auth#sub:refreshtoken"]
  }
}
```

### 索引生成工具

**文件**：`build-index-auto.js`

**使用方法**：

```bash
# 在项目根目录运行
node build-index-auto.js

# 或指定目录
node build-index-auto.js /path/to/project
```

**输出示例**：

```
═══════════════════════════════════════════════════════
  🚀 SKILL 索引生成工具 v3.0 (极简版 - 自动提取)
═══════════════════════════════════════════════════════
📁 项目目录: /path/to/project

📦 模块: auth (认证模块)
   文件: src/modules/auth/SKILL.md
   自动提取 21 个 sections:
   ├─ sub:login: "login(dto: LoginDto)" (79-120行)
   │  └─ auto-tags: login, dto, promise
   ├─ sub:register: "register(dto: RegisterDto)" (121-162行)
   │  └─ auto-tags: register, dto, promise

═══════════════════════════════════════════════════════
  📊 统计信息
═══════════════════════════════════════════════════════
  SKILL 文件数: 5
  索引模块数: 2
  自动提取 Section 数: 32
  标签总数: 61

✅ 索引已生成: SKILL.index.json
═══════════════════════════════════════════════════════
```

### 查询流程

**查询类问题**（2 步完成）：

```
用户问：怎么刷新 Token？

Step 1: 读取 SKILL.index.json
        → tagIndex["refreshtoken"] → auth#sub:refreshtoken
        → 得到 file + start/end 行号

Step 2: 读取目标内容
        → Read auth/SKILL.md (163-189行)

✅ 完成（~2,000 字符）
```

**开发类问题**（需要完整上下文）：

```
用户说：帮我新增重置密码功能

Step 1: 读取索引，定位 auth 模块
Step 2: 读取完整 auth/SKILL.md
        → 了解模块架构、设计模式、开发规范
Step 3: 参考现有 API 实现
Step 4: 开发新功能
Step 5: 更新 SKILL.md
Step 6: 重新生成索引
```

---

## 项目结构

### 标准目录结构

```
project-root/
├── SKILL.md                    # 第1层：项目根文档（必须）
├── SKILL.index.json            # 语义索引文件（自动生成）
├── build-index-auto.js         # 索引生成工具
├── README.md                   # 项目简介（面向 GitHub）
│
├── src/                        # 源代码目录
│   ├── SKILL.md               # 第2层：源码整体文档
│   │
│   ├── modules/               # 业务模块
│   │   ├── auth/
│   │   │   └── SKILL.md      # 第3层：模块文档
│   │   └── user/
│   │       └── SKILL.md      # 第3层：模块文档
│   │
│   └── core/                  # 核心模块
│       └── SKILL.md          # 第3层：核心模块文档
│
├── docs/                      # 文档中心
│   ├── SKILL.md              # 文档索引
│   ├── design/               # 设计文档
│   ├── planning/             # 开发计划
│   └── api/                  # API 详细文档
│
└── tests/                     # 测试目录
    └── SKILL.md              # 测试文档
```

### 层级策略

| 层级 | 位置 | 内容 | 必要性 |
|------|------|------|--------|
| 第1层 | 根目录 | 项目概述、技术栈、目录索引 | **必须** |
| 第2层 | src/, docs/ | 模块列表、架构说明 | 推荐 |
| 第3层 | 具体模块 | API 文档、开发规范 | 按需 |

**原则**：
- 最多 3 层，避免过深
- 宁缺毋滥，不确定就不加
- 以"是否帮助理解"为标准

---

## 文档维护

### 自动化工作流

```
代码变更 → 更新 SKILL.md → 运行 build-index-auto.js → 提交
```

### Git Hook 集成

```bash
# .git/hooks/pre-commit
#!/bin/sh
node build-index-auto.js
git add SKILL.index.json
```

### CI/CD 集成

```yaml
# .github/workflows/docs.yml
name: Update Index
on: [push]
jobs:
  update-index:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: node build-index-auto.js
      - run: |
          git config user.name "CI Bot"
          git add SKILL.index.json
          git commit -m "chore: update index" || true
          git push
```

---

## 效率对比

### 查询效率

| 方案 | 步骤数 | 字符消耗 | 节省比例 |
|------|--------|---------|---------|
| 全局搜索 | 5+ | 15,938 | - |
| 层级遍历 (v1.0) | 3 | 9,536 | 40% |
| **语义索引 (v2.0)** | **2** | **~2,000** | **87%** |

### 维护成本

| 方案 | 手动工作 | 自动化 |
|------|---------|--------|
| v1.0 | 维护行号、@key 标记 | 无 |
| **v2.0** | **只需 YAML 头部** | **自动提取 sections + tags** |

---

## 模板

### 根 SKILL.md 模板

```markdown
---
name: project-name
title: 项目名称
tags: [核心标签1, 核心标签2, 核心标签3]
---

# 项目名称

## 项目概述

- **目标**：项目目标和愿景
- **核心功能**：主要功能列表
- **目标用户**：目标用户群体

## 技术栈

- **语言**：[编程语言]
- **框架**：[主要框架]
- **数据库**：[数据库]
- **其他**：[其他工具]

## 项目架构

[整体架构说明]

## 目录结构

### src/
源代码目录
- 详见：[src/SKILL.md](src/SKILL.md)

### docs/
项目文档中心
- 详见：[docs/SKILL.md](docs/SKILL.md)

## 快速开始

1. 克隆仓库
2. 安装依赖
3. 配置环境
4. 启动服务

## 开发规范

- 代码规范：[链接]
- Git 规范：[链接]
- AI 开发指南：[AI开发指南v2.md](AI开发指南v2.md)
```

### 模块 SKILL.md 模板

```markdown
---
name: module-name
title: 模块名称
tags: [标签1, 标签2, 标签3]
---

# 模块名称

> 层级：第 N 层
> 上层：[上层文档](../SKILL.md)

## 模块概述

- **职责**：模块的主要职责
- **位置**：在项目中的位置
- **依赖**：依赖的其他模块

## 模块架构

[内部结构说明]

## API 文档

### ClassName

#### methodName(params): ReturnType

**功能说明**：功能描述

**参数**：
- `param1` (type): 参数说明

**返回值**：
- (type): 返回值说明

**使用示例**：

```language
// 示例代码
```

## 开发规范

### 错误处理
[规范说明]

### 日志记录
[规范说明]

## 依赖关系

### 外部依赖
- [库名]: 用途

### 内部依赖
- [模块名]: 用途
```

---

## 检查清单

### 新项目初始化

- [ ] 创建根 SKILL.md（含 YAML 头部）
- [ ] 复制 build-index-auto.js 到项目
- [ ] 运行索引生成工具
- [ ] 配置 Git Hook（可选）

### 新模块创建

- [ ] 创建模块 SKILL.md（含 YAML 头部）
- [ ] 编写模块概述和 API 文档
- [ ] 更新父级 SKILL.md 索引
- [ ] 重新生成索引

### 功能完成

- [ ] 更新 SKILL.md 的 API 部分
- [ ] 重新生成索引
- [ ] 验证示例代码可运行

---

## 版本历史

### v2.0.0 (2026-01-29)
- 新增 YAML 头部格式
- 新增语义索引系统
- 新增自动提取 sections 功能
- 新增 build-index-auto.js 工具
- 支持语言无关的通用方案
- Token 消耗降低 80%+

### v1.0.0 (2025-12-17)
- 初始版本
- 定义 SKILL.md 规范
- 定义 docs/ 目录规范
- 提供模板和最佳实践

---

## 参考资料

- [Claude Code Skill 机制](https://github.com/anthropics/claude-code)
- [测试报告](test-framework/测试报告.md)
- [索引工具源码](test-framework/build-index-auto.js)
