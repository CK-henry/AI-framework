# AI 协作旧项目迁移指南

> 将现有项目逐步迁移到 AI 协作文档体系

## 快速决策

| 你的情况 | 应该看 |
|---------|--------|
| 新项目，从 0 开始 | 👉 [AI协作文档建系指南.md](AI协作文档建系指南.md) |
| 已有项目，需要迁移 | ✅ 本文档 |
| 只想了解技术规范 | 👉 [项目文档规范标准v2.md](项目文档规范标准v2.md) |

---

# 一、核心目标

旧项目迁移的目标不是文档美化，而是 **AI 可协作化**：

- 让 AI 能更稳定地理解现有项目
- 让 AI 在修改时不再大量依赖全局猜测
- 让项目知识从”只有老开发者知道”变成”可写进文档与索引”
- 让 AI 对旧项目的修改更少误伤结构

---

# 二、迁移流程总览

```
阶段 1: 项目摸底
  ├─ AI 扫描项目结构
  └─ 识别迁移优先级 ✓

阶段 2: 建立最小骨架
  ├─ 补根项目 SKILL.md
  ├─ 补目录级 SKILL.md
  └─ 补 2-5 个关键模块 ✓

阶段 3: 生成索引并使用
  ├─ 生成 SKILL.index.json
  └─ AI 按体系工作 ✓

阶段 4: 边开发边补齐
  └─ 持续更新文档 ✓
```

**最小迁移版本**：根 SKILL.md + src/SKILL.md + docs/SKILL.md + 2-5 个关键模块 + 索引

---

# 三、核心原则

## 原则 1：不要试图一次性补完整个项目
旧项目最危险的做法：
- 一次性给所有目录补 `SKILL.md`
- 一次性让 AI 生成大量模块文档
- 一次性试图重写全部 docs

**结果**：生成很多空文档，体系比原来更乱，AI 从代码迷路变成文档迷路。

**正确做法**：分阶段，只覆盖最有价值的上下文节点。

## 原则 2：先服务于 AI 协作，再追求文档完整度
优先补的是：
- 核心模块
- 高变更模块
- 依赖复杂模块
- AI 最容易误改的模块

## 原则 3：先忠实描述现状，再逐步推动优化
不要把”希望未来变成什么样”直接写成”现在就是这样”。

文档首先应该描述现状，必要时再标明未来计划。

## 原则 4：AI 负责起草与扫描，人类负责边界确认
- **AI 负责**：发现、起草、整理
- **人类负责**：判断、裁剪、定边界

---

# 四、关键检查清单

## 阶段 1 检查清单：项目摸底

- [ ] 已识别核心目录和源码核心
- [ ] 已识别最复杂的模块
- [ ] 已识别 AI 容易误判的地方
- [ ] 已划分迁移优先级（高/中/低）
- [ ] **摸底完成** ✓

## 阶段 2 检查清单：建立最小骨架

- [ ] 根 SKILL.md 已完成
- [ ] src/SKILL.md 已完成
- [ ] docs/SKILL.md 已完成
- [ ] 2-5 个关键模块文档已完成
- [ ] **最小骨架就绪** ✓

## 阶段 3 检查清单：生成索引并使用

- [ ] SKILL.index.json 已生成
- [ ] AI 开始按索引定位代码
- [ ] **索引系统可用** ✓

## 阶段 4 检查清单：边开发边补齐

- [ ] 每次开发后同步更新文档
- [ ] 定期重新生成索引
- [ ] **持续维护中** ✓

---

# 五、阶段详细说明

## 阶段 1：项目摸底

### Step 1：让 AI 扫描当前项目结构
先让 AI 帮你回答这些问题：

- 根目录有哪些核心目录？
- 哪些目录是源码核心？
- 哪些模块最复杂？
- 哪些地方最容易让 AI 误判？
- docs 当前有哪些内容？

目标不是立刻写文档，而是先识别：

> **哪些位置是项目的高价值上下文节点。**

---

### Step 2：识别迁移优先级
让 AI 和人类一起划分：

#### 优先级最高
- 核心业务模块
- 高频修改模块
- 依赖复杂模块
- 容易误改模块

#### 优先级中等
- 重要目录级导航
- docs 分类导航
- 公共基础设施模块

#### 优先级较低
- 边缘目录
- 很少动的历史代码
- 临时工具目录

这一步是为了防止一上来全项目铺开。

---

## 阶段 2：建立最小迁移骨架

### Step 3：先补根项目 `SKILL.md`
先做项目总文档，至少说明：

- 项目目标
- 技术栈
- 核心目录
- 主要模块
- 现阶段项目边界

这是整个迁移的总入口。

---

### Step 4：再补目录级 `SKILL.md`
优先补：
- `src/SKILL.md`
- `docs/SKILL.md`
- 必要时补 `tests/`、`config/`、`scripts/` 等

这一步的目标是建立“第二层导航”，不是写细节大全。

---

### Step 5：挑 2～5 个关键模块建立模块文档
不要全铺。

先挑最关键的几个模块，例如：
- auth
- user
- payment
- order
- scheduler

每个模块先有最小文档：
- 职责
- 位置
- 依赖
- 主要 API / 能力

只要这个核心层一建起来，AI 的表现就会比之前稳定很多。

---

## 阶段 3：生成索引并开始使用

### Step 6：生成 `SKILL.index.json`
运行索引生成工具，把当前已有文档转成可检索结构。

这一刻很关键，因为它标志着体系从“文档存在”进入“AI 真能用”。

---

### Step 7：开始让 AI 按体系工作
从这一阶段开始，AI 在旧项目中应优先：

- 先读索引
- 再定位模块
- 再读相关文档
- 最后读代码

而不是回到之前那种“上来全文搜仓库”的模式。

---

## 阶段 4：边开发边补齐

### Step 8：随着开发逐步扩文档，不追求一口气补完
每次 AI 或人类开发一个模块、修一个复杂问题、重构一个目录时：

- 顺手补齐该处文档
- 更新模块职责
- 更新 API 描述
- 重新生成索引

这样体系会自然长出来，而不是靠一次大扫除撑起来。

---

# 五、旧项目迁移时 AI 应该做什么

## AI 适合做的

### 1. 扫描和分类
- 扫描目录
- 识别核心模块
- 归纳目录职责
- 标记高价值上下文节点

### 2. 起草文档初稿
- 根 `SKILL.md`
- 目录级 `SKILL.md`
- 模块级 `SKILL.md`
- docs 分类说明

### 3. 发现问题
- 发现缺失文档
- 发现边界模糊模块
- 发现文档与代码不一致的地方
- 发现重复职责和不清晰命名

### 4. 迁移时持续更新
- 开发后同步补文档
- 重构后同步调整目录导航
- 重新生成索引

---

## AI 不适合擅自做的

### 1. 擅自重划整个项目架构
旧项目往往有历史包袱，不是“最优结构”就能立刻改。

### 2. 一次性为所有目录生成完整文档
这通常会把噪音一起写进体系。

### 3. 把猜测写成事实
AI 看几份代码，可能以为某个模块负责某事；
但在旧项目里，这种判断必须谨慎。

### 4. 混淆“当前现状”和“未来理想结构”
如果不分清，后面改代码时就会出事。

---

# 六、旧项目迁移时人类必须确认什么

AI 可以很能干，但旧项目迁移里，有几类事情必须人来拍板：

## 1. 当前项目边界
- 这次迁移覆盖到什么范围？
- 哪些目录先不碰？
- 哪些历史结构暂时保留？

## 2. 核心模块优先级
- 哪些模块必须先补文档？
- 哪些模块暂时不值得写？

## 3. 现状与目标的区别
- 哪些是当前真实情况？
- 哪些只是目标方向？

## 4. 历史包袱是否顺便处理
有些旧结构虽然很丑，但当前阶段可能不值得边迁移边重构。

这点必须人工判断，不能让 AI 一拍脑袋全改了。

---

# 七、旧项目迁移最常见的坑

## 坑 1：一上来全铺文档
最后得到一大堆半成品文档，没人愿意维护。

## 坑 2：把理想架构写成当前现状
结果文档和代码对不上，AI 继续误改。

## 坑 3：只写项目总览，不写模块文档
这样 AI 还是会在核心模块层迷路。

## 坑 4：迁移完不更新
第一次迁移很认真，后面项目一动，文档又废了。

## 坑 5：旧文档不删，新文档又加
最后出现双份说明互相打架，AI 读哪个都不对劲。

---

# 八、推荐的最小迁移版本

如果你想让旧项目尽快接入这套体系，最小可行版本可以是：

1. 根 `SKILL.md`
2. `src/SKILL.md`
3. `docs/SKILL.md`
4. 2～5 个关键模块 `SKILL.md`
5. `SKILL.index.json`

只要这几样建立起来，AI 在旧项目中的稳定性就会明显提升。

这比“一次性铺全项目”更现实，也更容易成功。

---

# 九、最后结论

旧项目迁移不是新项目建系的附属版本，
而是一套单独的工作流。

它的关键不是“写更多文档”，而是：

- 识别最关键的上下文节点
- 先建立最小可用体系
- 先服务于 AI 协作稳定性
- 让文档忠实描述现状
- 在后续开发中持续补齐和更新

如果说新项目建系像“从地基开始搭楼”，
那旧项目迁移更像：

> **一边住人，一边给老房子补结构、补图纸、补说明，而且还不能把房子搞塌。**

这就是为什么它必须单独写成一份指南。